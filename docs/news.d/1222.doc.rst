The website now publishes a machine-readable reference of the VHDL and Python APIs for AI agents
(``api/vunit_lib.json`` and ``api/python.json``). ``AGENTS.md`` and ``context7.json`` were added to the repository.
